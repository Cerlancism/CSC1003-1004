/* Classic include guard */
#ifndef NAVIGATOR_H
#define NAVIGATOR_H

extern int navigate(const char *const controller, float *xAxis, float *yAxis, float *scale, unsigned int *width, unsigned int *height);

#endif
